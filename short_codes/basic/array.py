#Deve ser usada quando a lista e muito grande e ha problema de performance no codigo.
from array import array

letters = array("w", ["m", "a", "r", "c", "o", "s"]) #Deve ser utilizado o unicode(Tabela disponivel na documentacao), nesse caso o 'w', representando string.
print(letters)